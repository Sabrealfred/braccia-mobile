import 'package:flutter/material.dart';
import '_stub.dart';

class MessagesScreen extends StatelessWidget {
  const MessagesScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const StubScaffold(title: 'Messages', icon: Icons.forum);
}
