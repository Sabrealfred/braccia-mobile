import 'package:flutter/material.dart';
import '_stub.dart';

class CreditStackScreen extends StatelessWidget {
  const CreditStackScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const StubScaffold(title: 'Credit Stack', icon: Icons.workspace_premium);
}
