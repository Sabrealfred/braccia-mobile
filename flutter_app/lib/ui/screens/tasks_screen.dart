import 'package:flutter/material.dart';
import '_stub.dart';

class TasksScreen extends StatelessWidget {
  const TasksScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const StubScaffold(title: 'Tasks', icon: Icons.checklist);
}
