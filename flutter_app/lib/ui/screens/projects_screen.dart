import 'package:flutter/material.dart';
import '_stub.dart';

class ProjectsScreen extends StatelessWidget {
  const ProjectsScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const StubScaffold(title: 'Projects', icon: Icons.view_kanban);
}
