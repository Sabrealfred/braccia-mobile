import 'package:flutter/material.dart';
import '_stub.dart';

class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const StubScaffold(title: 'Settings', icon: Icons.settings);
}
