import 'package:flutter/material.dart';
import '_stub.dart';

class AiScreen extends StatelessWidget {
  const AiScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const StubScaffold(title: 'Braccia AI', icon: Icons.auto_awesome);
}
