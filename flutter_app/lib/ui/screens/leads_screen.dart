import 'package:flutter/material.dart';
import '_stub.dart';

class LeadsScreen extends StatelessWidget {
  const LeadsScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const StubScaffold(title: 'Leads', icon: Icons.person_add_alt_1);
}
