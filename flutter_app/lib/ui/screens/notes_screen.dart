import 'package:flutter/material.dart';
import '_stub.dart';

class NotesScreen extends StatelessWidget {
  const NotesScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const StubScaffold(title: 'Notes', icon: Icons.sticky_note_2);
}
